//Accedere a proprietà usando il punto e salvare il valore in una variabile

const car = {
    brand : 'Toyota',
    year : 2020
}

const carBrand = car.brand;

console.log(carBrand);

const carYear = car.year;

console.log(carYear);

//Aggiungere una proprietà a un oggetto e salvare il risultato in una variabile
const student = {
    name : 'Anna',
    enrolled : 'True',
}

const isEnrolled = student.enrolled;
console.log(isEnrolled);