// 1: Crea e stampa un array con tipi di dati diversi  
let mixedData = ['apple', 42, true];
console.log(mixedData);
console.log(mixedData[2]);

// 2: Memorizza un array in una variabile e accedi agli elementi  
let studentNames = ['Anna', 'Luca', 'Maria'];
console.log(studentNames[0]);
console.log(studentNames.length);
