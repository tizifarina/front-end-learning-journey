//1: Stampa i numeri da 3 a 0 in ordine decrescente usando un ciclo `for`
for(let count = 3; count >= 0; count--){
    console.log(count);
}

// 2: Stampa solo numeri dispari in ordine decrescente da 9 a 1 usando un ciclo `for`
for(let oddNum = 9; oddNum >= 1; oddNum--){
    console.log(oddNum);
}