//1: Iterare su un array di città e stamparle

let cityList = ['Rome', 'Milan', 'Naples', 'Turin'];

for(let i = 0; i < cityList.length; i++){
    console.log(cityList[i]);
}

// 2: Stampare le lunghezze dei nomi dei clienti

let customerNames = ['Anna', 'Giovanni', 'Elena', 'Marco'];

for(let i = 0; i < customerNames.length; i++){
    console.log(customerNames[i].length);
}